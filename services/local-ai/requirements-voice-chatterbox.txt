aiortc>=1.9.0,<2
av>=12.0.0
websockets>=12.0
faster-whisper==1.2.1
ctranslate2>=4.7.0,<5
# Do not pin nvidia-* CUDA wheels here. chatterbox-tts pulls torch 2.6,
# and that torch build pins the compatible CUDA 12.4 wheel set. The gateway's
# LD_LIBRARY_PATH helper can use the cublas/cudnn wheels installed by torch.
chatterbox-tts==0.1.7
numpy>=1.24.0,<2
soundfile>=0.12.1
